import styled from 'styled-components';

export const Container = styled.div`
  margin-top: calc(100vh / 2 - 9rem);
  display: grid;
  place-items: center;
  gap: 1rem;

  color: var(--white-400);

  user-select: none;
`;
